# electron-builder configuration
appId: com.you.app.id
directories:
  app: 'dist'
  buildResources: 'build/resources'
  output: 'releases'
files:
  # Do not incluse the sourcemaps!
  - '!**/*.map'
  # We dont need node_modules
  - '!node_modules'
